{
  "name": "Nira AI — Android (Capacitor wrapper)",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper around the Nira React+Vite PWA. Reuses the existing ui/ frontend build.",
  "scripts": {
    "build:web": "cd ../ui && npm install && npm run build",
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:apk": "npx cap build android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/device": "^6.0.0",
    "@capacitor/network": "^6.0.0"
  }
}
